<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="shortcut icon" href="">

  <title><%= titulo %></title>
  <meta name="description" content="">
  <meta name="keywords" content="">
  <meta name="author" content="">

  <!-- Font -->
  <link href="./stylesheets/open_sans.css" rel="stylesheet">

  <!-- Estilo bootstrap -->
  <link rel="stylesheet" href="./libraries/bootstrap_v4/css/bootstrap.min.css">
  <!-- Arquivo de estilo -->
  <link rel="stylesheet" href="./stylesheets/style.css">
  <!-- Webfonts icons do google -->
  <link rel="stylesheet" href="./libraries/webfont/css/materialdesignicons.min.css">

  <!-- Inclusão por tag js (ex.: include='caminho') -->
  <script src="./libraries/include/myInclude.js" charset="utf-8"></script>
</head>
<body class="bg-page">
  <!-- Seu site -->

  <header class="text-dark" include="/include/header.html"></header>

  <section class="container py-5 mt-5 px-4 list-home-eventos">
    <div class="row">

      <!-- <div class="col-12 col-sm-6 col-lg-4">
        <a href="javascript:cardMaisEventos()" class="card card-evento card-mais-eventos w-100 mb-3 text-light border-0 rounded-0">
          <div class="card-body text-center d-flex flex-column align-items-center justify-content-center">
            <span class="mdi mdi-plus-box pt-0 pb-2 px-0 m-0 text-warning" style="line-height: 50px"></span>
            <img src="./images/spin.svg" alt="" width="50px" height="55px" class="pt-0 pb-2 m-0">
            <h5 class="font-weight-normal text-truncate m-0">TODOS OS EVENTOS</h5>
          </div>
        </a>
      </div> -->
    </div>
  </section>

  <section include="/include/preFooter.html"></section>

  <footer include="/include/footer.html"></footer>


  <!-- JavaScript jquery -->
  <script src="./libraries/jquery/jquery-3.1.1.min.js" charset="utf-8"></script>
  <!-- JavaScript popper -->
  <script src="./libraries/bootstrap_v4/js/popper.min.js" charset="utf-8"></script>
  <!-- JavaScript bootstrap -->
  <script src="./libraries/bootstrap_v4/js/bootstrap.min.js" charset="utf-8"></script>

  <script src="./javascripts/root.js" charset="utf-8"></script>
  <script src="./javascripts/diretivas.js" charset="utf-8"></script>

  <script type="text/javascript">
  window.onload = function () {
    $('.btn-nav-home').addClass('active')
  }
  $(function () {
    var $list
    var $boxLista = $(".list-home-eventos .row")
    var control = null

    $.ajax({
      url: hostApi+'/evento/lista',
      cache: true,
    }).done(function (e) {
      (e.dados).forEach(function (elem) {
        elem.imagem = hostApi+'/image/'+elem.imagem
        $boxLista.append(dir.cardEvento(elem))
      })
      $list = $(".card-evento")
    }).fail(function () {
      console.log('Error');
    })

    $(document).keydown(function(e){
      if (e.which === 37 || e.which === 39 || e.which === 27) {
        if (control === null) { /* Inicializa o focus nos cards */
          control = 0
          $list[control].focus()
        }else{

          if(e.which === 27){ /* Tecla ESC */
            $list[control].blur()

            return false
          }
          if(e.which === 37){ /* Tecla para esquerda */
            if ($list[control-1]) {
              control = control-1
              $list[control].focus()
            }

            return false
          }
          if(e.which === 39){ /* Tecla para direita */
            if ($list[control+1]) {
              control = control+1
              $list[control].focus()
            }

            return false
          }
        }
      }
    })
  })

  function cardMaisEventos() {
    var elem = $('.card-mais-eventos')

    elem.hasClass('active') ? elem.removeClass('active') : elem.addClass('active')
  }
  </script>
</body>
</html>
